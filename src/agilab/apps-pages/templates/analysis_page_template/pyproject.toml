[project]
name = "view-demo"
version = "0.1.0"
description = "AGILAB analysis page template"
requires-python = ">=3.11"
dependencies = ["streamlit>=1.56.0"]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
