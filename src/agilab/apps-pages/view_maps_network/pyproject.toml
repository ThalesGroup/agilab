[project]
name = "view-maps-network"
version = "2025.12.12"
requires-python = ">=3.13"
dependencies = [
    "agi-gui",
    "agi-node",
    "networkx",
    "plotly",
    "matplotlib",
    "sqlalchemy>=2.0.43",
]

[tool.uv.sources]
agi-gui = { path = "../../lib/agi-gui", editable = true }
agi-env = { path = "../../core/agi-env", editable = true }
agi-node = { path = "../../core/agi-node", editable = true }
