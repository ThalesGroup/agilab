[[notebook_app_project]]
D = "Text Atlas – Dimensionality Reduction of Text Data"
Q = "Run the reviewed notebook cells in order, preserving their shared Python state."
C = "# AGILAB notebook execution: original cell order, one Python namespace.\n(lambda _execute, _compile, _namespace, _cells: [\n    _execute(_compile(_source, _filename, 'exec', incomplete_input=False), _namespace)\n    for _source, _filename in _cells\n])(__import__('builtins').exec, __import__('codeop').Compile(), globals(),\n   [('import json\\nfrom pathlib import Path\\nimport numpy as np\\nimport pandas as pd\\n\\nfrom text_core import load_corpus, analyze', 'notebook:cell-2'), ('# Stage 1: Load corpus\\ndf = load_corpus()\\nprint(f\"Loaded {len(df)} documents, {df[\\'category\\'].nunique()} categories\")\\nprint(df.head())\\n\\n# Stage 2: Run analysis\\nresult = analyze()\\nprint(f\"Vocabulary: {len(result[\\'vocabulary\\'])} terms\")\\nprint(f\"Silhouette (50-D): {result[\\'silhouette\\']:.4f}\")\\nprint(f\"Displayed variance (2-D): {result[\\'displayed_variance\\']:.4f}\")\\nprint(f\"Retained variance (50-D): {result[\\'retained_variance\\']:.4f}\")\\n\\n# Stage 3: Write results.json to current execution directory\\nresults = {\\n    \"results\": {\\n        \"coordinates\": result[\"coordinates\"].tolist(),\\n        \"labels\": result[\"labels\"].tolist(),\\n        \"vocabulary\": result[\"vocabulary\"],\\n        \"top_terms\": result[\"top_terms\"],\\n        \"silhouette\": result[\"silhouette\"],\\n        \"displayed_variance\": result[\"displayed_variance\"],\\n        \"retained_variance\": result[\"retained_variance\"],\\n        \"parameters\": result[\"parameters\"],\\n    }\\n}\\n\\nout_path = Path(\"results.json\")\\nout_path.write_text(json.dumps(results, indent=2))\\nprint(f\"Wrote {out_path}\")', 'notebook:cell-3')])\n"
M = ""
NB_CELL_ID = "notebook-93688c92d68a4011"
NB_CELL_INDEX = 2
NB_CONTEXT_IDS = [
    "markdown-1",
]
NB_ENV_HINTS = [
    "json",
    "numpy",
    "pandas",
    "pathlib",
    "text_core",
]
NB_ARTIFACT_REFERENCES = [
    "results.json",
]
NB_EXECUTION_MODE = "not_executed_import"
NB_SOURCE_NOTEBOOK = "solution.ipynb"
NB_EXECUTION_PLAN_SCHEMA = "agilab.notebook_execution_plan.v1"
NB_COMPILED_SHA256 = "6e3c01dd3ff6a3591e0ba34753517a5e72aeec45c7189dfb6681baa4239956d7"
NB_EXECUTION_STRATEGY = "shared_namespace"
NB_SOURCE_SHA256 = "93688c92d68a4011702c509001cf4c73204f2a5e60a186b5271454b9f1ae633d"

[[notebook_app_project.NB_SOURCE_CELLS]]
id = "cell-2"
index = 2
source = "import json\nfrom pathlib import Path\nimport numpy as np\nimport pandas as pd\n\nfrom text_core import load_corpus, analyze"
sha256 = "adbb904d889344781aba7b8e792f5f6406e7d93adf58945062df0a69b2117f33"

[[notebook_app_project.NB_SOURCE_CELLS]]
id = "cell-3"
index = 3
source = "# Stage 1: Load corpus\ndf = load_corpus()\nprint(f\"Loaded {len(df)} documents, {df['category'].nunique()} categories\")\nprint(df.head())\n\n# Stage 2: Run analysis\nresult = analyze()\nprint(f\"Vocabulary: {len(result['vocabulary'])} terms\")\nprint(f\"Silhouette (50-D): {result['silhouette']:.4f}\")\nprint(f\"Displayed variance (2-D): {result['displayed_variance']:.4f}\")\nprint(f\"Retained variance (50-D): {result['retained_variance']:.4f}\")\n\n# Stage 3: Write results.json to current execution directory\nresults = {\n    \"results\": {\n        \"coordinates\": result[\"coordinates\"].tolist(),\n        \"labels\": result[\"labels\"].tolist(),\n        \"vocabulary\": result[\"vocabulary\"],\n        \"top_terms\": result[\"top_terms\"],\n        \"silhouette\": result[\"silhouette\"],\n        \"displayed_variance\": result[\"displayed_variance\"],\n        \"retained_variance\": result[\"retained_variance\"],\n        \"parameters\": result[\"parameters\"],\n    }\n}\n\nout_path = Path(\"results.json\")\nout_path.write_text(json.dumps(results, indent=2))\nprint(f\"Wrote {out_path}\")"
sha256 = "a716f5f00688b5ddc894635f5bfb160dca58f82e346bb37f3da46dfaa6bf323f"
