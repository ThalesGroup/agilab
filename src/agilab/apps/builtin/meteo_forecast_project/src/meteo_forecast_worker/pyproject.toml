[project]
name = "meteo_forecast_project"
version = "2026.05.23"
description = "Built-in AGILab weather forecasting example migrated from notebooks"
requires-python = ">=3.13"
dependencies = ["agi-env>=2026.05.23,<2027.0", "agi-node>=2026.05.23,<2027.0", "pandas>=2.3.0,<4", "pydantic>=2.11,<3", "scikit-learn>=1.7.2,<2", "skforecast>=0.19,<0.20"]

[tool.uv.sources]
agi-env = { path = "../../../../../core/agi-env", editable = true }
agi-node = { path = "../../../../../core/agi-node", editable = true }

[tool.setuptools.package-data]
meteo_forecast = ["sample_data/*.csv"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
