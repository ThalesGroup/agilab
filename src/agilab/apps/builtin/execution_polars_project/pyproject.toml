[project]
name = "execution_polars_project"
version = "2026.04.06.post1"
description = "Built-in AGILab execution playground using PolarsWorker"
requires-python = ">=3.11"
dependencies = [
  "pydantic",
  "polars",
  "streamlit>=1.55.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
