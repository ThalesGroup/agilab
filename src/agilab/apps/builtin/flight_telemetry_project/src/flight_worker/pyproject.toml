[project]
name = "flight_telemetry_project"
version = "2026.05.16"
description = ""
requires-python = ">=3.11,<3.14"
readme = "README.md"
dependencies = ["agi-env>=2026.05.16", "agi-node>=2026.05.16", "polars", "pydantic"]

[[project.authors]]
name = "Jean-Pierre Morard"
email = "focus@thalesgroup.com"


[project.license]
text = "Thales SIX GTS France SAS"

[project.optional-dependencies]
dag-worker = []
polars-worker = []



[tool.uv.sources]
agi-env = { path = "../../../../../core/agi-env", editable = true }
agi-node = { path = "../../../../../core/agi-node", editable = true }

[build-system]
requires = ["setuptools", "cython"]
build-backend = "setuptools.build_meta"












[dependency-groups]
dev = [
    "black",
    "pytest",
    "pytest-asyncio",
    "licensecheck",
]
