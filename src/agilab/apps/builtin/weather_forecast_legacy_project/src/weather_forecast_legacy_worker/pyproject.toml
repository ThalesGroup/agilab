[project]
name = "weather_forecast_legacy_project"
version = "2026.05.30.post1"
description = "Built-in AGILab weather forecasting example migrated from notebooks"
requires-python = ">=3.13"
dependencies = ["agi-env>=2026.05.30.post1", "agi-node>=2026.05.30.post1", "pandas>=2.3.0,<4", "pydantic>=2.11,<2.13", "scikit-learn>=1.7.2,<2", "skforecast>=0.19,<0.20"]

[tool.uv.sources]
agi-env = { path = "../../../../../core/agi-env", editable = true }
agi-node = { path = "../../../../../core/agi-node", editable = true }

[tool.setuptools.package-data]
weather_forecast_legacy = ["sample_data/*.csv"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
